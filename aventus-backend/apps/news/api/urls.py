from django.urls import include, path, re_path
from . import views


urlpatterns = [
    # re_path(r'^news/', include([
    #     path('get-all-news', views.GetAllNewsView.as_view()),
    #     path('get-news', views.NewsListingApiView.as_view()),
    # ])),
]
